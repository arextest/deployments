receivers:
  otlp:
    protocols:
      grpc:
      http:


exporters:
  skywalking:
    endpoint: "10.5.153.151:11800"
    tls:
      insecure: true  
  jaeger:
    endpoint: jaeger:14250
    tls:
      insecure: true
  zipkin:
    endpoint: "http://zipkin:9411/api/v2/spans"
    format: proto
  opencensus:
    endpoint: "0.0.0.0:11800" # The OAP Server address
    tls:
      insecure: true
  logging:
    logLevel: debug

processors:
  batch:

extensions:
  health_check:
  pprof:
  zpages:

service:
  extensions: [pprof, zpages, health_check]
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [jaeger, logging, zipkin, skywalking]
      processors: [batch]
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [logging, opencensus]
